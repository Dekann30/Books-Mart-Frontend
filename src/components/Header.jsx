import styled from 'styled-components'

const StyledHeader = styled.header`
    color: black;
` 

export default function Header() {
    return (
        <header>
            <h1>Book Mart</h1>
        </header>
    )
}